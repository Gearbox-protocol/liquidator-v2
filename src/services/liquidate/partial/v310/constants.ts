import { type Curator, NOT_DEPLOYED } from "@gearbox-protocol/sdk";
import type { Address } from "viem";

// TODO: ...
export const V310_PARTIAL_LIQUIDATOR_BOTS: Record<Curator, Address> = {
  K3: NOT_DEPLOYED,
  "Chaos Labs": NOT_DEPLOYED,
};
